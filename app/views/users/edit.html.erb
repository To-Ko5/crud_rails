<%= form_for @user, url: "/users/#{@user.id}" , html: {method: :patch} do |f| %>
  <p>編集画面</p>
  <p>お名前<%= f.text_field :name %></p>
  <p>年齢<%= f.text_field :age %></p>
  <p>お名前<%= f.text_field :email %></p>
  <p><input type="submit" value="編集する"></p>
<% end %>